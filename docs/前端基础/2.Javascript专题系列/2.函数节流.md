---
title: '函数节流'
date: 2020-03-26
permalink: 'js_topic/func_throttle'
tag:
  - javascript专题系列
categories:
  - javascript专题系列
---

```js
const throttle = function(func, wait) {
  let timer;

  return function() {
    let context = this;
    let args = arguments;

    if (!timer) {
      timer = setTimeout(() => {
        func.apply(context, args);
        clearTimeout(timer);
        timer = null;
      }, wait);
    }
  }
}
```
