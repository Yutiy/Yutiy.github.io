---
title: 'jenkins'
date: 2020-03-25
permalink: '/jenkins'
tag:
  - test
categories:
  - test
---

## jekins前言

```shell
export NODEJS_PATH=/var/jenkins_home/node-v14.8.0-linux-x64/bin
export PATH=$NODEJS_PATH:$PATH
echo "PATH: ${PATH}"

node -v
npm -v

pwd

npm install -g yarn -registry=https://registry.npm.taobao.org
yarn -v
yarn install
yarn run build:test

tar -zcvf dist.tar.gz dist

pwd
ls
```
